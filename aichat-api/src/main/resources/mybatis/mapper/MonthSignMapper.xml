<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.luowei.aichat.dao.mapper.MonthSignMapper">

    <resultMap id="BaseResultMap" type="io.luowei.aichat.dao.po.MonthSign">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="openid" column="openid" jdbcType="VARCHAR"/>
            <result property="signMonth" column="sign_month" jdbcType="VARCHAR"/>
            <result property="signRecord" column="sign_record" jdbcType="BIGINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,openid,sign_month,
        sign_record
    </sql>

    <select id="selectByOpenidAndMonth"  resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from month_sign
        where  openid = #{openid} and sign_month = #{month}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from month_sign
        where  id = #{id,jdbcType=BIGINT} 
    </delete>
    <insert id="insert" keyColumn="id" keyProperty="id" parameterType="io.luowei.aichat.dao.po.MonthSign" useGeneratedKeys="true">
        insert into month_sign
        ( openid,sign_month,sign_record)
        values (#{openid},#{signMonth},#{signRecord})
    </insert>
    <insert id="insertSelective" keyColumn="id" keyProperty="id" parameterType="io.luowei.aichat.dao.po.MonthSign" useGeneratedKeys="true">
        insert into month_sign
        <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="id != null">id,</if>
                <if test="openid != null">openid,</if>
                <if test="signMonth != null">sign_month,</if>
                <if test="signRecord != null">sign_record,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="id != null">#{id,jdbcType=BIGINT},</if>
                <if test="openid != null">#{openid,jdbcType=VARCHAR},</if>
                <if test="signMonth != null">#{signMonth,jdbcType=VARCHAR},</if>
                <if test="signRecord != null">#{signRecord,jdbcType=BIGINT},</if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="io.luowei.aichat.dao.po.MonthSign">
        update month_sign
        <set>
                <if test="openid != null">
                    openid = #{openid,jdbcType=VARCHAR},
                </if>
                <if test="signMonth != null">
                    sign_month = #{signMonth,jdbcType=VARCHAR},
                </if>
                <if test="signRecord != null">
                    sign_record = #{signRecord,jdbcType=BIGINT},
                </if>
        </set>
        where   id = #{id,jdbcType=BIGINT} 
    </update>
    <update id="updateByPrimaryKey" parameterType="io.luowei.aichat.dao.po.MonthSign">
        update month_sign
        set
            sign_record =  #{signRecord,jdbcType=BIGINT}
        where   openid =  #{openid,jdbcType=VARCHAR} and sign_month =  #{signMonth,jdbcType=VARCHAR}
    </update>
</mapper>
